# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  base_url: a15k.org
  secret_key_base: ac81bcc86427b3e4ecf5d54185bd5441ae0a2dad5ad92e52c5bcd99eac29351fd48a329baef5a6fe5ba9bff3a8594b843f1874e1e0edfc99006f592a4f2caec4
  openstax:
    accounts:
      client_id: <%= ENV["OPENSTAX_ACCOUNTS_CLIENT_ID"] %>
      secret: <%= ENV["OPENSTAX_ACCOUNTS_SECRET"] %>
      url: <%= ENV["OPENSTAX_ACCOUNTS_URL"] || 'http://localhost:2999' %>
      stub: <%= ENV.fetch("OPENSTAX_ACCOUNTS_STUB", true) %>

test:
  base_url: a15k.org
  secret_key_base: af8b9fab193891504598d3720c18c3109c4756fd6c3dc53c01d683b02d649cb384cc0fd6dac3f1812939bde7c9634b2633833cf209a9b11a13c0bf3c9cd00404
  openstax:
    accounts:
      client_id: <%= ENV["OPENSTAX_ACCOUNTS_CLIENT_ID"] %>
      secret: <%= ENV["OPENSTAX_ACCOUNTS_SECRET"] %>
      url: <%= ENV["OPENSTAX_ACCOUNTS_URL"] || 'http://localhost:2999' %>
      stub: <%= ENV.fetch("OPENSTAX_ACCOUNTS_STUB", true) %>

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
